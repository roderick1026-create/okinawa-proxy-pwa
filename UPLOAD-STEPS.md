# 沖繩代購 V4.0.3 上傳順序

## 1. 更新 Apps Script

1. 在 Apps Script 專案中開啟 `AppsScript-Code.gs`，全選後以本資料夾同名檔案完整覆蓋。
2. 開啟 `AppsScript-Api.gs`，同樣完整覆蓋。
3. 儲存後按「部署」→「管理部署」→選既有 Web App→「編輯」→建立新版本→「部署」。
4. 保持既有 Web App URL，不需要修改前端網址。

## 2. 更新 GitHub Pages

以本資料夾的 `index.html` 與 `sw.js` 覆蓋 GitHub repository 同名檔案並提交。

## 3. 開啟新版

開啟正式 PWA 後按一次「更新」。若仍看到舊畫面，關閉網頁後重新開啟即可。

## 功能重點

- 相同商品採買時，系統依喊單順序自動分配給顧客。
- 每位顧客可用加減按鈕手動調整，且不能超過需求或本次採買數量。
- 更正、撤銷採買，以及修改或取消訂單後，後端會重新計算受影響商品的分配。
- 採買分配會保存於獨立的「採買分配」工作表，不會混入備註欄位。
- 匯率、操作人與版本資訊集中在可摺疊的「設定與匯率」。
- 採買完成提示會列出商品、數量、顧客分配與總成本。
- 收款頁底部會顯示所有有效未收訂單的筆數與總額。
- 同商品有多位顧客時，建立喊單後會保留商品與價格，只清空顧客及數量。
- 收款頁會依顧客彙整，並可一次將該顧客所有未收訂單標記為已收款。
- 採買頁預設為快速清單模式，可切換回管理卡片；多人顧客需求與分配預設收合。
- 頁首會清楚顯示目前操作人，降低兩人共用時記錯人的機會。
- 相同商品但喊單日幣售價不同時，會自動分開採買、分配與成本；不需新增規格欄位。
- 修改需求時會顯示該筆訂單的喊單日本售價。
